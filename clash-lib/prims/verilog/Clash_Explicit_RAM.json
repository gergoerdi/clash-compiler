[ { "BlackBox" :
    { "name" : "Clash.Explicit.RAM.asyncRam#"
    , "kind" : "Declaration"
    , "type" :
"asyncRam#
  :: HasCallStack      --         ARG[0]
  => Undefined a       --         ARG[1]
  => Clock wdom wgated -- ^ wclk, ARG[2]
  -> Clock rdom rgated -- ^ rclk, ARG[3]
  -> SNat n            -- ^ sz,   ARG[4]
  -> Signal rdom Int   -- ^ rd,   ARG[5]
  -> Signal wdom Bool  -- ^ en,   ARG[6]
  -> Signal wdom Int   -- ^ wr,   ARG[7]
  -> Signal wdom a     -- ^ din,  ARG[8]
  -> Signal rdom a"
    , "template" :
"// asyncRam begin
reg ~TYPO ~GENSYM[RAM][0] [0:~LIT[4]-1];
~IF ~ISGATED[2] ~THEN
always @(posedge ~ARG[2][1]) begin : ~GENSYM[~COMPNAME_Ram][1]
  if (~ARG[6] & ~ARG[2][0]) begin
    ~SYM[0][~ARG[7]] <= ~ARG[8];
  end
end~ELSE
always @(posedge ~ARG[2]) begin : ~SYM[1]
  if (~ARG[6]) begin
    ~SYM[0][~ARG[7]] <= ~ARG[8];
  end
end~FI

assign ~RESULT = ~SYM[0][~ARG[5]];
// asyncRam end"
    }
  }
]
