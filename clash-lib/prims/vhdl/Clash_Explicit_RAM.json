[ { "BlackBox" :
    { "name" : "Clash.Explicit.RAM.asyncRam#"
    , "kind" : "Declaration"
    , "type" :
"asyncRam#
  :: HasCallStack      --         ARG[0]
  => Undefined a       --         ARG[1]
  => Clock wdom wgated -- ^ wclk, ARG[2]
  -> Clock rdom rgated -- ^ rclk, ARG[3]
  -> SNat n            -- ^ sz,   ARG[4]
  -> Signal rdom Int   -- ^ rd,   ARG[5]
  -> Signal wdom Bool  -- ^ en,   ARG[6]
  -> Signal wdom Int   -- ^ wr,   ARG[7]
  -> Signal wdom a     -- ^ din,  ARG[8]
  -> Signal rdom a"
    , "template" :
"-- asyncRam begin
~GENSYM[~COMPNAME_asyncRam][0] : block~IF ~VIVADO ~THEN
  type ~GENSYM[RamType][4] is array(natural range <>) of std_logic_vector(~SIZE[~TYP[8]]-1 downto 0);~ELSE
  type ~SYM[4] is array(natural range <>) of ~TYP[8];~FI
  signal ~GENSYM[RAM][1] : ~SYM[4](0 to ~LIT[4]-1);
  signal ~GENSYM[rd][2] : integer range 0 to ~LIT[4] - 1;
  signal ~GENSYM[wr][3] : integer range 0 to ~LIT[4] - 1;~IF ~ISGATED[2] ~THEN
  signal ~GENSYM[clk][5] : std_logic;
  signal ~GENSYM[ce][6] : boolean;~ELSE ~FI
begin
  ~SYM[2] <= to_integer(~ARG[5])
  -- pragma translate_off
                mod ~LIT[4]
  -- pragma translate_on
                ;

  ~SYM[3] <= to_integer(~ARG[7])
  -- pragma translate_off
                mod ~LIT[4]
  -- pragma translate_on
                ;
  ~IF ~ISGATED[2] ~THEN
  (~SYM[5],~SYM[6]) <= ~ARG[2];
  ~GENSYM[asyncRam_sync][7] : process(~SYM[5])
  begin
    if rising_edge(~SYM[5]) then
      if (~ARG[6] and ~SYM[6]) then~IF ~VIVADO ~THEN
        ~SYM[1](~SYM[3]) <= ~TOBV[~ARG[8]][~TYP[8]];~ELSE
        ~SYM[1](~SYM[3]) <= ~ARG[8];~FI
      end if;
    end if;
  end process;~ELSE
  ~SYM[7] : process(~ARG[2])
  begin
    if rising_edge(~ARG[2]) then
      if ~ARG[6] then~IF ~VIVADO ~THEN
        ~SYM[1](~SYM[3]) <= ~TOBV[~ARG[8]][~TYP[8]];~ELSE
        ~SYM[1](~SYM[3]) <= ~ARG[8];~FI
      end if;
    end if;
  end process;~FI
  ~IF ~VIVADO ~THEN
  ~RESULT <= ~FROMBV[~SYM[1](~SYM[2])][~TYP[8]];~ELSE
  ~RESULT <= ~SYM[1](~SYM[2]);~FI
end block;
-- asyncRam end"
    }
  }
]
